{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:integration": "vitest run tests/integration"
  },
  "dependencies": {
    "lodash-es": "^4.17"
  },
  "devDependencies": {
    "jsdom": "^24.0",
    "vitest": "^2.0"
  }
}
